auth    [success=1 default=ignore]      pam_unix.so nullok_secure debug

auth    requisite                       pam_deny.so debug
auth    required                        pam_permit.so debug

auth    required                        pam_naumotp.so debug
